{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently -n \"backend,frontend\" -c \"magenta,cyan\" \"php artisan serve\" \"npm run dev --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}

